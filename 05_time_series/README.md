# Segment 05 — Time Series Forecasting (Baselines + Lag Features)

This segment uses a synthetic time series (trend + seasonality + noise) and compares:
- Naive forecast (last value)
- Moving average forecast
- Linear regression with lag features (autoregressive rollout)

## Run
```powershell
python 05_time_series/train.py
